import { EventoMouse } from './evento-mouse';

describe('EventoMouse', () => {
  it('should create an instance', () => {
    const directive = new EventoMouse();
    expect(directive).toBeTruthy();
  });
});
